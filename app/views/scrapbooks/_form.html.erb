<%= form_for(@scrapbook) do |f| %>
  <% if @scrapbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scrapbook.errors.count, "error") %> prohibited this scrapbook from being saved:</h2>

      <ul>
      <% @scrapbook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
